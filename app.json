{
  "name": "frank_mail",
  "scripts": {
  },
  "env": {
    "LANG": {
      "required": true
    },
    "POSTMARK_API_TOKEN": {
      "required": true
    },
    "RACK_ENV": "development",
    "RAILS_ENV": "development",
    "RAILS_SERVE_STATIC_FILES": {
      "required": true
    },
    "SECRET_KEY_BASE": {
      "generator": "secret"
    }
  },
  "addons": [
    "papertrail",
    "heroku-postgresql"
  ],
  "buildpacks": [
    {
      "url": "urn:buildpack:heroku/ruby"
    }
  ]
}
